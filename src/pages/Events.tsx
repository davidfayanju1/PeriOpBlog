import DefaultLayout from "../layout/DefaultLayout";

const Events = () => {
  return (
    <DefaultLayout>
      <div className="title">Events</div>
    </DefaultLayout>
  );
};

export default Events;
